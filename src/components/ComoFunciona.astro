---
/* Sin props: contenido fijo según el fragmento */
// import auto from "../../public/auto.png"; // <- eliminar
---

<section id="COMO-FUNCIONA" class="scroll-mt-16 py-16 sm:py-20 bg-white">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <!-- Títulos -->
    <header class="text-center max-w-2xl mx-auto">
      <p class="text-4xl font-bold tracking-wide text-[var(--bp-ink-muted)]">
        Cómo funciona
      </p>
      <h2 class="mt-2 text-xl md:text-2xl font-bold text-[var(--bp-ink)]">
        Tres simples pasos
      </h2>
    </header>

    <!-- Pasos -->
    <div class="mt-12 grid gap-10 md:grid-cols-3">
      <!-- Paso 1 -->
      <article class="flex flex-col items-center text-center gap-4 px-4">
        <!-- Badge numerado -->
        <div
          role="img"
          aria-label="Paso 1"
          class="relative grid place-items-center rounded-full font-extrabold text-white
                 w-[4.3rem] h-[4.3rem] md:w-[4.8rem] md:h-[4.8rem] lg:w-[5.3rem] lg:h-[5.3rem]
                 text-3xl md:text-4xl lg:text-5xl
                 bg-gradient-to-br from-[var(--bp-primary-300)] to-[var(--bp-support)]
                 ring-4 ring-[var(--bp-surface)] shadow-xl
                 hover:scale-[1.03] transition-transform duration-200"
        >
          1
          <span
            class="pointer-events-none absolute inset-0 rounded-full blur-2xl opacity-30"
            style="background: radial-gradient(40% 40% at 50% 50%, var(--bp-support) 0%, transparent 70%);"
          ></span>
        </div>

        <h3 class="mt-2 text-xl md:text-2xl font-bold text-[var(--bp-ink)]">
          Reserva
        </h3>
        <p class="text-[var(--bp-ink-muted)] leading-relaxed text-[1.3rem]">
          <span class="font-semibold text-[var(--bp-ink)]"
            >Contactanos por WhatsApp</span
          > y te responderemos a la brevedad.
        </p>
      </article>

      <!-- Paso 2 -->
      <article class="flex flex-col items-center text-center gap-4 px-4">
        <div
          role="img"
          aria-label="Paso 2"
          class="relative grid place-items-center rounded-full font-extrabold text-white
                 w-[4.3rem] h-[4.3rem] md:w-[4.8rem] md:h-[4.8rem] lg:w-[5.3rem] lg:h-[5.3rem]
                 text-3xl md:text-4xl lg:text-5xl
                 bg-gradient-to-br from-[var(--bp-primary-300)] to-[var(--bp-support)]
                 ring-4 ring-[var(--bp-surface)] shadow-xl
                 hover:scale-[1.03] transition-transform duration-200"
        >
          2
          <span
            class="pointer-events-none absolute inset-0 rounded-full blur-2xl opacity-30"
            style="background: radial-gradient(40% 40% at 50% 50%, var(--bp-support) 0%, transparent 70%);"
          ></span>
        </div>

        <h3 class="mt-2 text-xl md:text-2xl font-bold text-[var(--bp-ink)]">
          Retiro del vehículo
        </h3>
        <p class="text-[var(--bp-ink-muted)] leading-relaxed text-[1.3rem]">
          Recibimos tu vehículo según <span
            class="font-semibold text-[var(--bp-ink)]"
            >fecha y horario pautado </span> en la terminal del <span
            class="font-semibold text-[var(--bp-ink)]"
            >aeropuerto internacional Ezeiza</span
          >.
        </p>
      </article>

      <!-- Paso 3 -->
      <article class="flex flex-col items-center text-center gap-4 px-4">
        <div
          role="img"
          aria-label="Paso 3"
          class="relative grid place-items-center rounded-full font-extrabold text-white
                 w-[4.3rem] h-[4.3rem] md:w-[4.8rem] md:h-[4.8rem] lg:w-[5.3rem] lg:h-[5.3rem]
                 text-3xl md:text-4xl lg:text-5xl
                 bg-gradient-to-br from-[var(--bp-primary-300)] to-[var(--bp-support)]
                 ring-4 ring-[var(--bp-surface)] shadow-xl
                 hover:scale-[1.03] transition-transform duration-200"
        >
          3
          <span
            class="pointer-events-none absolute inset-0 rounded-full blur-2xl opacity-30"
            style="background: radial-gradient(40% 40% at 50% 50%, var(--bp-support) 0%, transparent 70%);"
          ></span>
        </div>

        <h3 class="tmt-2 text-xl md:text-2xl font-bold text-[var(--bp-ink)]">
          Entrega del vehículo
        </h3>
        <p class="text-[var(--bp-ink-muted)] leading-relaxed text-[1.3rem]">
          Entregamos tu vehículo <span
            class="font-semibold text-[var(--bp-ink)]"
            >según fecha y horario pautado</span
          >.
        </p>
      </article>
    </div>

    <!-- Botón -->
    <div class="mt-12 text-center">
      <a
        href="/reservar"
        class="mt-6 text-center w-[10%] rounded-md bg-[--bp-support] px-6 py-3 text-white font-semibold btn-accent"
        data-cta-observer>Reserva</a
      >
    </div>
  </div>
</section>
